# Contributing

Bitte Änderungen klein halten, Security-relevante Änderungen mit Tests abdecken und keine Clearnet-Fallbacks in Tor-Pfade einbauen. Vor einem Pull Request `python scripts/check_release.py` ausführen.
